name: Patch student routine view buttons

on:
  push:
    paths:
      - 'scripts/patch_student_routine_view_buttons.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Patch student routine view buttons
        run: python scripts/patch_student_routine_view_buttons.py
      - name: Commit patch
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add alumno/index.html
          git commit -m "Add routine exercise view buttons"
          git push
