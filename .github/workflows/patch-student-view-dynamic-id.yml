name: Patch student view dynamic id

on:
  push:
    paths:
      - 'scripts/patch_student_view_dynamic_id.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Patch student view dynamic id
        run: python scripts/patch_student_view_dynamic_id.py
      - name: Commit patch
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add alumno/index.html
          git commit -m "Allow student view by id parameter"
          git push
